
终端现实层  |  开放接口
请求处理层(Web层）  |    ||
业    务    逻    辑    层(Service层)
     ||    |   通用处理层(Managerceng)
数据持久层(DAO层)         ||
数据源   |  外部接口或第三方平台


注
1. 默认上层依赖与下层.
2. "||" 表示可以直接依赖.

1). 开放接口层: 可直接封装成Service方法暴露成RPC接口; 通过Web封装层HTTP接口; 进行网关安全控制, 流量控制等.
2). 终端现实层: 各个端的模版渲染并执行显示的层. 当前主要是Velocity渲染, JS渲染, JSP渲染, 移动端渲染.
3). Web层: 主要是对访问控制进行转发,对各类基本参数的校验, 或者对不复用的业务简单处理.
4). Service层: 相对具体的业务逻辑服务层.
5). Manager层: 通用业务处理层. 它有如下特征:
    - 对第三方平台封装的层, 预处理返回结果以及转化异常信息.
    - 对Service层通用能力的下层, 如缓存方案, 中间价通用处理.
    - 与DAO层交互, 对多个DAO的组合复用.
6). DAO层: 数据访问层, 与底层Mysql, Oracle, HBase 等进行数据交互.
7). 外部接口或第三方平台: 包括其他部门RPC开发接口, 基础平台, 其他公司的HTTP接口.


#TODO 在项目中文件夹对应关系: